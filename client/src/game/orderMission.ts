import{OO_ASSESSMENT,OO_LESSON_1,OO_LESSON_2,type OoQuestion}from"./orderOperationsUnit8";export const O_IDS=["operation-orbit","story-sprint","order-master"]as const;export type OId=(typeof O_IDS)[number];export type OLesson=Exclude<OId,"order-master">;export type OLevel="easy"|"normal"|"hard";export const O_ROUTES=[{id:"operation-orbit",lesson:"LESSON 1",title:"Operation Orbit",icon:"×",accent:"#8068F6",soft:"#EAE6FF"},{id:"story-sprint",lesson:"LESSON 2",title:"Story Sprint",icon:"=",accent:"#FF7958",soft:"#FFE2D9"},{id:"order-master",lesson:"UNIT 8 MASTER",title:"Order Master Challenge",icon:"★",accent:"#F6B41D",soft:"#FFF0C8",master:true}]as const;type Route=typeof O_ROUTES[number];export const oRoute=(id:OId)=>O_ROUTES.find(x=>x.id===id)!;export type OQuestion={id:string;prompt:string;correct:string;choices:readonly[string,string,string,string];labels:readonly string[];level:OLevel};const num=(a:string)=>{const n=Number(a.replace(/[^0-9.-]/g,""));return Number.isFinite(n)&&a.match(/^[-\d,.]+$/)?[a,String(n+1),String(n+2),String(Math.max(0,n-1))]:[a,"Not enough information","0","1"]};const adapt=(x:OoQuestion,i:number):OQuestion=>{const level=i<15?"easy":i<30?"normal":"hard" as OLevel;const c=[...num(x.answer)].filter((v,j,a)=>a.indexOf(v)===j);return{id:x.id,prompt:x.prompt,correct:x.answer,choices:c.slice(0,4)as[string,string,string,string],labels:x.labels,level}};const map=(b:readonly OoQuestion[])=>b.map(adapt);export const O_BANK={"operation-orbit":map(OO_LESSON_1),"story-sprint":map(OO_LESSON_2),"order-master":OO_ASSESSMENT.slice(0,15).map((x,i)=>({...adapt(x,i),level:"hard"as OLevel}))}as const;export const oQuestions=(id:OId,l?:OLevel)=>id==="order-master"?[...O_BANK[id]]:[...O_BANK[id]].filter(x=>x.level===l);const ids:OLesson[]=["operation-orbit","story-sprint"],key="mosy-order-unit8-v1";type S=Record<OLesson,{done:OLevel[]}>;const fresh=()=>Object.fromEntries(ids.map(id=>[id,{done:[]}]))as unknown as S;export const oRead=():S=>{try{return typeof window==="undefined"?fresh():{...fresh(),...JSON.parse(localStorage.getItem(key)??"{}")} }catch{return fresh()}};export const oMaster=(s:S)=>ids.every(id=>s[id].done.includes("hard"));export const oRecord=(id:OLesson,l:OLevel)=>{const s=oRead();if(!s[id].done.includes(l))s[id].done.push(l);localStorage.setItem(key,JSON.stringify(s))};
