import{describe,expect,it}from"vitest";import{UNIT7_ASSESSMENT as bank}from"../client/src/game/unit7Assessment";describe("Unit Seven Assessment source bank",()=>{it("validates representative answers",()=>{expect(bank.find(x=>x.id==="u7a-oranges")?.expectedAnswer).toBe("2");expect(bank.find(x=>x.id==="u7a-7425")?.expectedAnswer).toBe("1,485")});it("keeps city labels and multiple-choice keys",()=>{expect(bank.flatMap(x=>x.sourceLabels)).toContain("Cairo – Heliopolis 23");bank.filter(x=>x.choices).forEach(x=>expect(x.choices).toContain(x.expectedAnswer))})});
