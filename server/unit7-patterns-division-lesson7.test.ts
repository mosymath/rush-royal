import{describe,expect,it}from"vitest";import{UNIT7_PATTERNS_IN_DIVISION_LESSON_7 as bank}from"../client/src/game/unit7PatternsInDivisionLesson7";describe("Unit 7 Part 2 Lesson 7 source bank",()=>{it("validates division patterns and missing values",()=>{expect(bank.find(x=>x.id==="l7-coins")?.expectedAnswer).toBe("2,000");expect(bank.find(x=>x.id==="l7-blankd7")?.expectedAnswer).toBe("42,000")});it("keeps city labels and selectable keys",()=>{expect(bank.flatMap(x=>x.sourceLabels)).toContain("Cairo – El-Salam 23");bank.filter(x=>x.choices).forEach(x=>expect(x.choices).toContain(x.expectedAnswer))})});
