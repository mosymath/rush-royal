import{describe,expect,it}from"vitest";import{UNIT7_DIVISION_ALGORITHMS_LESSONS_9_AND_10 as bank}from"../client/src/game/unit7DivisionAlgorithmsLessons9and10";describe("Unit 7 Part 2 Lessons 9–10 source bank",()=>{it("validates partial quotients, standard division, and remainders",()=>{expect(bank.find(x=>x.id==="l9-cakes")?.expectedAnswer).toBe("224 R2");expect(bank.find(x=>x.id==="l10-1765d4")?.expectedAnswer).toBe("441 R1")});it("keeps past-exam labels and source choices",()=>{expect(bank.flatMap(x=>x.sourceLabels)).toContain("Cairo – El-Marg 23");bank.filter(x=>x.choices).forEach(x=>expect(x.choices).toContain(x.expectedAnswer))})});
