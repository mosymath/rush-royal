import{describe,expect,it}from"vitest";import{UNIT7_AREA_MODEL_DIVISION_LESSON_8 as bank}from"../client/src/game/unit7AreaModelDivisionLesson8";describe("Unit 7 Part 2 Lesson 8 source bank",()=>{it("validates area-model quotients and remainders",()=>{expect(bank.find(x=>x.id==="l8-drive")?.expectedAnswer).toBe("205");expect(bank.find(x=>x.id==="l8-825d4")?.expectedAnswer).toBe("206 R1")});it("keeps source-labels and choice answers",()=>{expect(bank.flatMap(x=>x.sourceLabels)).toContain("Cairo – El-Salam 23");bank.filter(x=>x.choices).forEach(x=>expect(x.choices).toContain(x.expectedAnswer))})});
