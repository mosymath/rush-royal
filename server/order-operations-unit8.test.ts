import{describe,expect,it}from"vitest";import{OO_ASSESSMENT,OO_LESSON_1,OO_LESSON_2}from"../client/src/game/orderOperationsUnit8";describe("Unit 8 Order of Operations source bank",()=>{it("has 45 distinct source prompts in both lessons",()=>{expect(OO_LESSON_1).toHaveLength(45);expect(OO_LESSON_2).toHaveLength(45);expect(new Set(OO_LESSON_1.map(x=>x.prompt)).size).toBe(45);expect(new Set(OO_LESSON_2.map(x=>x.prompt)).size).toBe(45)});it("keeps assessment answers and city labels",()=>{expect(OO_ASSESSMENT.find(x=>x.id==="a4")?.answer).toBe("17");expect(OO_ASSESSMENT.flatMap(x=>x.labels)).toContain("Cairo – El-Nozha 23")})});
