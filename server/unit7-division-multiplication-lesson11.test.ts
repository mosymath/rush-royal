import{describe,expect,it}from"vitest";import{UNIT7_DIVISION_MULTIPLICATION_LESSON_11 as bank}from"../client/src/game/unit7DivisionMultiplicationLesson11";describe("Unit 7 Part 2 Lesson 11 source bank",()=>{it("validates inverse facts and check answers",()=>{expect(bank.find(x=>x.id==="l11-crayons")?.expectedAnswer).toBe("184");expect(bank.find(x=>x.id==="l11-mc-check")?.expectedAnswer).toBe("28 × 9 + 4")});it("preserves city labels and choices",()=>{expect(bank.flatMap(x=>x.sourceLabels)).toContain("Giza – Abo El-Nomros 23");bank.filter(x=>x.choices).forEach(x=>expect(x.choices).toContain(x.expectedAnswer))})});
